import 'package:flutter/material.dart';

class AboutPage extends StatelessWidget {
  const AboutPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('About Page - Coming Soon'));
}

class ChangePasswordPage extends StatelessWidget {
  const ChangePasswordPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Change Password Page - Coming Soon'));
}

class MessagePage extends StatelessWidget {
  const MessagePage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Message Page - Coming Soon'));
}

class AnnouncementPage extends StatelessWidget {
  const AnnouncementPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Announcement Page - Coming Soon'));
}

class RulesRegulationsPage extends StatelessWidget {
  const RulesRegulationsPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Rules & Regulations Page - Coming Soon'));
}

class ShortcutsPage extends StatelessWidget {
  const ShortcutsPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Shortcuts Page - Coming Soon'));
}

class TotalVolumePage extends StatelessWidget {
  const TotalVolumePage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Total Volume Page - Coming Soon'));
}
